# Deno algosdk/purestake

Simple health-check using `algosdk` and purestake.io testnet using `deno`

Assumes `deno` is already installed.

## Run

```bash
deno run --import-map imports.json index.js
```
